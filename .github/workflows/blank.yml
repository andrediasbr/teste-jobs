name: CI
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo Build

  deploy-tu:
    environment: tu
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy TU
        run: echo Deploy TU

